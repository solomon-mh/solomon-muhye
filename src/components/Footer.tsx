const Footer = () => {
  return (
    <footer className="py-6 text-center text-sm text-gray-500 dark:text-gray-400">
      &copy; {new Date().getFullYear()} Solomon. Built with ❤️ and React.
    </footer>
  );
};

export default Footer;
